<UserControl x:Class="TM.Modules.Validate.ValidationIntro.PlotIntro.PlotIntroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:layout="clr-namespace:TM.Framework.Common.Controls.Layout"
             xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <emoji:TextBlock Text="📖" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="剧情校验介绍" FontSize="24" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="剧情校验用于确保小说中的剧情设定保持逻辑性和连贯性。包含以下校验项：" 
                       Foreground="{DynamicResource TextSecondary}" 
                       TextWrapping="Wrap" Margin="0,0,0,20" FontSize="14"/>

            <layout:IconHeaderGroupBox Icon="🔗" Title="剧情逻辑校验" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="校验目的" FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12">
                        检查剧情发展是否符合逻辑，因果关系是否合理，避免剧情漏洞。
                    </TextBlock>

                    <TextBlock Text="校验内容" FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                    <StackPanel Margin="10,0,0,0">
                        <TextBlock Text="• 因果逻辑：事件之间的因果关系是否合理" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="• 时间线：剧情时间顺序是否正确" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="• 前置条件：事件发生是否具备必要条件" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </layout:IconHeaderGroupBox>

            <layout:IconHeaderGroupBox Icon="⚔️" Title="冲突与转折校验" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="校验目的" FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12">
                        验证核心冲突设计是否有效，转折点是否有足够铺垫和冲击力。
                    </TextBlock>

                    <TextBlock Text="校验内容" FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                    <StackPanel Margin="10,0,0,0">
                        <TextBlock Text="• 冲突有效性：核心冲突是否能推动剧情发展" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="• 转折铺垫：重大转折是否有足够的伏笔" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="• 解决合理性：冲突解决方式是否令人信服" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </layout:IconHeaderGroupBox>

            <layout:IconHeaderGroupBox Icon="🎯" Title="事件影响校验" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="校验目的" FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12">
                        审查重要事件的后续影响是否得到体现，确保剧情的连贯性。
                    </TextBlock>

                    <TextBlock Text="校验内容" FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                    <StackPanel Margin="10,0,0,0">
                        <TextBlock Text="• 后果呈现：重大事件是否产生应有的影响" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="• 伏笔回收：埋下的伏笔是否得到合理回收" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="• 主线推进：事件是否有效推动主线发展" 
                                   Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </layout:IconHeaderGroupBox>

            <Border Background="{DynamicResource InfoBackground}" 
                    BorderBrush="{DynamicResource InfoBorder}" 
                    BorderThickness="1" CornerRadius="6" Padding="15">
                <StackPanel Orientation="Horizontal">
                    <emoji:TextBlock Text="💡" FontSize="16" VerticalAlignment="Top" Margin="0,2,10,0"/>
                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}">
                        剧情校验结果可在「校验汇总 / 校验结果」页面查看。
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
