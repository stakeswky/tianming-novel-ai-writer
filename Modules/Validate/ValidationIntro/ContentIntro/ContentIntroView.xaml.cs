using System.Reflection;
using System.Windows.Controls;

namespace TM.Modules.Validate.ValidationIntro.ContentIntro
{
    [Obfuscation(Exclude = true, ApplyToMembers = true)]
    public partial class ContentIntroView : UserControl
    {
        public ContentIntroView()
        {
            InitializeComponent();
        }
    }
}
