<UserControl x:Class="TM.Framework.Common.Controls.Markdown.MarkdownStreamViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <ScrollViewer x:Name="StreamScrollViewer" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Visibility="Visible">
            <TextBlock x:Name="StreamTextBlock"
                       TextWrapping="Wrap"
                       FontSize="13"
                       FontFamily="Microsoft YaHei, Segoe UI"
                       Foreground="{DynamicResource TextPrimary}"
                       LineHeight="20"
                       Padding="5"/>
        </ScrollViewer>

        <ScrollViewer x:Name="MarkdownScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Visibility="Collapsed">
            <FlowDocumentScrollViewer x:Name="MarkdownDocument"
                                      VerticalScrollBarVisibility="Hidden"
                                      HorizontalScrollBarVisibility="Hidden"
                                      IsToolBarVisible="False"
                                      Background="Transparent"
                                      Foreground="{DynamicResource TextPrimary}"/>
        </ScrollViewer>
    </Grid>
</UserControl>

