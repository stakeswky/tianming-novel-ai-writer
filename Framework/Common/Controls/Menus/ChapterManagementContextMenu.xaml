<ContextMenu x:Class="TM.Framework.Common.Controls.Menus.ChapterManagementContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
             xmlns:local="clr-namespace:TM.Framework.Common.Controls.Menus">

    <ContextMenu.Template>
        <ControlTemplate TargetType="ContextMenu">
            <Border CornerRadius="8"
                    Background="{DynamicResource ContentBackground}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    Padding="4"
                    SnapsToDevicePixels="True">
                <ScrollViewer CanContentScroll="True"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto">
                    <ItemsPresenter/>
                </ScrollViewer>
            </Border>
        </ControlTemplate>
    </ContextMenu.Template>

    <MenuItem Header="删除"
              Command="{Binding Tag.InternalDeleteCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
        <MenuItem.Icon>
            <emoji:TextBlock Text="🗑️" FontSize="13"/>
        </MenuItem.Icon>
    </MenuItem>

    <MenuItem Header="刷新"
              Command="{Binding Tag.RefreshCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
        <MenuItem.Icon>
            <emoji:TextBlock Text="🔄" FontSize="13"/>
        </MenuItem.Icon>
    </MenuItem>

    <MenuItem Header="全部删除"
              Command="{Binding Tag.InternalDeleteAllCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
        <MenuItem.Icon>
            <emoji:TextBlock Text="🧹" FontSize="13"/>
        </MenuItem.Icon>
    </MenuItem>

    <Separator/>

    <MenuItem Header="新建章节"
              Command="{Binding Tag.InternalAddChapterCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
        <MenuItem.Icon>
            <emoji:TextBlock Text="📝" FontSize="13"/>
        </MenuItem.Icon>
    </MenuItem>

</ContextMenu>
