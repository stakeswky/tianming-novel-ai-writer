<ContextMenu x:Class="TM.Framework.Common.Controls.Menus.DataManagementContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TM.Framework.Common.Controls.Menus">

    <ContextMenu.Template>
        <ControlTemplate TargetType="ContextMenu">
            <Border CornerRadius="8"
                    Background="{DynamicResource ContentBackground}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    Padding="4"
                    SnapsToDevicePixels="True">
                <ScrollViewer CanContentScroll="True"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto">
                    <ItemsPresenter/>
                </ScrollViewer>
            </Border>
        </ControlTemplate>
    </ContextMenu.Template>

    <MenuItem Header="{Binding Tag.EnableSelectedHeader, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"
              Command="{Binding Tag.InternalEnableSelectedCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
        <MenuItem.Style>
            <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                <Setter Property="Visibility" Value="Visible"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding DataContext, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </MenuItem.Style>
    </MenuItem>

    <MenuItem Header="新建"
              Command="{Binding Tag.InternalAddCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
    </MenuItem>

    <MenuItem Header="编辑"
              Command="{Binding Tag.InternalEditCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
        <MenuItem.Style>
            <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                <Setter Property="Visibility" Value="Visible"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding DataContext, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </MenuItem.Style>
    </MenuItem>

    <MenuItem Header="删除"
              Command="{Binding Tag.InternalDeleteCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
    </MenuItem>

    <Separator/>

    <MenuItem Header="刷新"
              Command="{Binding Tag.RefreshCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
    </MenuItem>

    <MenuItem Header="全部删除"
              Command="{Binding Tag.InternalDeleteAllCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
    </MenuItem>

</ContextMenu>
