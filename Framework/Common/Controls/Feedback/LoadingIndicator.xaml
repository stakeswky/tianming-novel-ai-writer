<UserControl x:Class="TM.Framework.Common.Controls.Feedback.LoadingIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="48" Height="48">

    <UserControl.Resources>
        <Storyboard x:Key="SpinnerAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="SpinnerEllipse"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                           From="0" To="360" Duration="0:0:1"/>
        </Storyboard>

        <Storyboard x:Key="DotsAnimation" RepeatBehavior="Forever">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Dot1" Storyboard.TargetProperty="Opacity">
                <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="1"/>
                <DiscreteDoubleKeyFrame KeyTime="0:0:0.3" Value="0.3"/>
                <DiscreteDoubleKeyFrame KeyTime="0:0:0.9" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Dot2" Storyboard.TargetProperty="Opacity">
                <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="0.3"/>
                <DiscreteDoubleKeyFrame KeyTime="0:0:0.3" Value="1"/>
                <DiscreteDoubleKeyFrame KeyTime="0:0:0.6" Value="0.3"/>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Dot3" Storyboard.TargetProperty="Opacity">
                <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="0.3"/>
                <DiscreteDoubleKeyFrame KeyTime="0:0:0.6" Value="1"/>
                <DiscreteDoubleKeyFrame KeyTime="0:0:0.9" Value="0.3"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="PulseEllipse"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                           From="0.5" To="1.2" Duration="0:0:0.8" AutoReverse="True"/>
            <DoubleAnimation Storyboard.TargetName="PulseEllipse"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                           From="0.5" To="1.2" Duration="0:0:0.8" AutoReverse="True"/>
            <DoubleAnimation Storyboard.TargetName="PulseEllipse"
                           Storyboard.TargetProperty="Opacity"
                           From="0.8" To="0.3" Duration="0:0:0.8" AutoReverse="True"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="RootGrid">
        <Ellipse x:Name="SpinnerEllipse" 
                 Stroke="{DynamicResource PrimaryColor}" 
                 StrokeThickness="4" 
                 StrokeDashArray="6 4"
                 RenderTransformOrigin="0.5,0.5"
                 Visibility="Collapsed">
            <Ellipse.RenderTransform>
                <RotateTransform Angle="0"/>
            </Ellipse.RenderTransform>
        </Ellipse>

        <StackPanel x:Name="DotsPanel" Orientation="Horizontal" HorizontalAlignment="Center" Visibility="Collapsed">
            <Ellipse x:Name="Dot1" Width="10" Height="10" Fill="{DynamicResource PrimaryColor}" Margin="2"/>
            <Ellipse x:Name="Dot2" Width="10" Height="10" Fill="{DynamicResource PrimaryColor}" Margin="2"/>
            <Ellipse x:Name="Dot3" Width="10" Height="10" Fill="{DynamicResource PrimaryColor}" Margin="2"/>
        </StackPanel>

        <Ellipse x:Name="PulseEllipse"
                 Fill="{DynamicResource PrimaryColor}"
                 RenderTransformOrigin="0.5,0.5"
                 Visibility="Collapsed">
            <Ellipse.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Ellipse.RenderTransform>
        </Ellipse>

        <Ellipse x:Name="RingEllipse"
                 Stroke="{DynamicResource PrimaryColor}"
                 StrokeThickness="6"
                 Visibility="Collapsed"/>

        <TextBlock x:Name="PlaceholderText" 
                   Text="Loading..." 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</UserControl>

