<UserControl x:Class="TM.Framework.Notifications.NotificationManagement.NotificationHistory.NotificationHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
             xmlns:layout="clr-namespace:TM.Framework.Common.Controls.Layout">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <layout:IconHeaderGroupBox Grid.Row="0" Icon="📊" Title="统计信息" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="总记录：" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalCount}" FontWeight="SemiBold" 
                               Foreground="{DynamicResource PrimaryColor}" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="未读：" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding UnreadCount}" FontWeight="SemiBold" 
                               Foreground="{DynamicResource WarningColor}" Margin="5,0,0,0"/>
                </StackPanel>

                <Button Grid.Column="2" Content="清空全部" 
                        Command="{Binding ClearAllCommand}"
                        Style="{StaticResource DangerButtonStyle}"
                        Height="32" Padding="12,0"/>
            </Grid>
        </layout:IconHeaderGroupBox>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" 
                     Text="{Binding SearchKeyword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Height="36"/>

            <ComboBox Grid.Column="1" 
                      SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"
                      Width="120" Height="36" Margin="10,0,0,0">
                <ComboBoxItem Content="全部"/>
                <ComboBoxItem Content="未读"/>
                <ComboBoxItem Content="系统"/>
                <ComboBoxItem Content="任务"/>
                <ComboBoxItem Content="错误"/>
            </ComboBox>
        </Grid>

        <ListBox Grid.Row="2" 
                 ItemsSource="{Binding Records}"
                 Background="Transparent"
                 BorderThickness="0"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 ScrollViewer.CanContentScroll="True">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource Surface}" 
                            CornerRadius="8" 
                            Padding="15" 
                            Margin="0,0,0,10"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource BorderBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" 
                                    Width="8" Height="8" 
                                    CornerRadius="4" 
                                    Background="{DynamicResource WarningColor}"
                                    VerticalAlignment="Top"
                                    Margin="0,5,12,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRead}" Value="False">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Content}" FontSize="12" 
                                           Foreground="{DynamicResource TextSecondary}" 
                                           Margin="0,4,0,0" TextWrapping="Wrap"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Border Background="{DynamicResource PrimaryColor}" 
                                            CornerRadius="3" Padding="6,2" Margin="0,0,8,0">
                                        <TextBlock Text="{Binding Type}" FontSize="10" Foreground="White"/>
                                    </Border>
                                    <TextBlock Text="{Binding TimeDisplay}" FontSize="11" 
                                               Foreground="{DynamicResource TextTertiary}" 
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top">
                                <Button Content="标为已读" 
                                        Command="{Binding DataContext.MarkReadCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Height="28" Padding="10,0" Margin="0,0,8,0">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRead}" Value="False">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Height="28" Width="28" Padding="0">
                                    <emoji:TextBlock Text="🗑️" FontSize="14"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
