<UserControl x:Class="TM.Framework.UI.Workspace.CenterPanel.ChapterEditor.PlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf">

    <Border Background="{DynamicResource ContentBackground}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{DynamicResource Surface}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,6">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <emoji:TextBlock Text="📋" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="执行计划"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   Foreground="{DynamicResource TextPrimary}"
                                   Margin="6,0,0,0"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,0,0">
                        <Button Content="{Binding LeftButtonText}"
                                Padding="10,2"
                                Margin="0,0,4,0"
                                FontSize="14"
                                Command="{Binding ModifyOrCancelCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"/>
                        <Button Content="{Binding RightButtonText}"
                                Padding="10,2"
                                FontSize="14"
                                Command="{Binding StartOrSaveCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource DangerColor}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource DangerColor}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock Text="{Binding Events.Count, StringFormat=事件数：{0}}"
                                   Foreground="{DynamicResource TextSecondary}"
                                   FontSize="12"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ListBox ItemsSource="{Binding EventsView}"
                                 SelectedItem="{Binding SelectedEvent, Mode=TwoWay}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="0,5,0,0">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Margin" Value="0,0,3,3"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Background" Value="{DynamicResource ContentBackground}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="6"
                                                        Padding="{TemplateBinding Padding}"
                                                        Margin="{TemplateBinding Margin}">
                                                    <ContentPresenter/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource SelectedBackground}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                        <Ellipse Width="6" Height="6" Margin="0,0,6,0"
                                                 VerticalAlignment="Center">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" Value="{DynamicResource InfoColor}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding EventType}" Value="RunStarted">
                                                            <Setter Property="Fill" Value="{DynamicResource PrimaryColor}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding EventType}" Value="RunCompleted">
                                                            <Setter Property="Fill" Value="{DynamicResource SuccessColor}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding EventType}" Value="RunFailed">
                                                            <Setter Property="Fill" Value="{DynamicResource DangerColor}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Succeeded}" Value="False">
                                                            <Setter Property="Fill" Value="{DynamicResource DangerColor}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="12">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Succeeded}" Value="False">
                                                                <Setter Property="Foreground" Value="{DynamicResource DangerColor}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock FontSize="10" Foreground="{DynamicResource TextSecondary}">
                                                <Run Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.ContainerStyle>
                                        <Style TargetType="GroupItem">
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Padding" Value="0"/>
                                        </Style>
                                    </GroupStyle.ContainerStyle>
                                    <GroupStyle.HeaderTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource UnifiedBackground}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="0,0,0,1"
                                                    Padding="8,2"
                                                    Margin="5,0,5,5">
                                                <TextBlock FontSize="11"
                                                           Foreground="{DynamicResource TextSecondary}">
                                                    <Run Text="步骤 "/>
                                                    <Run Text="{Binding Name, Mode=OneWay}"/>
                                                </TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </GroupStyle.HeaderTemplate>
                                    <GroupStyle.Panel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Margin="0"/>
                                        </ItemsPanelTemplate>
                                    </GroupStyle.Panel>
                                </GroupStyle>
                            </ListBox.GroupStyle>
                        </ListBox>
                    </ScrollViewer>
                </Border>

                <Border Grid.Column="1" Background="{DynamicResource BorderBrush}"/>

                <Grid Grid.Column="2" Margin="8">
                    <Grid Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" 
                                   Text="编辑计划内容（修改后点击「保存计划」生效）"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextSecondary}"
                                   Margin="0,0,0,8"/>

                        <Border Grid.Row="1" 
                                BorderBrush="{DynamicResource PrimaryColor}" 
                                BorderThickness="2" 
                                CornerRadius="4">
                            <TextBox Text="{Binding PlanContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     FontSize="13"
                                     Padding="8"
                                     Background="{DynamicResource ContentBackground}"
                                     Foreground="{DynamicResource TextPrimary}"
                                     BorderThickness="0"/>
                        </Border>
                    </Grid>

                    <Grid Visibility="{Binding IsEditMode, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,4">
                            <TextBlock Text="{Binding RunSummaryText}"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextSecondary}"
                                       Margin="0,0,0,2"/>

                            <TextBlock Text="{Binding ErrorSummaryText}"
                                       FontSize="11"
                                       Foreground="{DynamicResource DangerColor}"
                                       Margin="0,0,0,4"
                                       TextWrapping="Wrap"/>

                            <TextBlock Text="{Binding SelectedEvent.Title}"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimary}"
                                       TextTrimming="CharacterEllipsis"/>

                            <TextBlock FontSize="10"
                                       Foreground="{DynamicResource TextSecondary}"
                                       Margin="0,2,0,0"
                                       Text="{Binding SelectedEvent.Timestamp, StringFormat={}{0:HH:mm:ss}}"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextSecondary}"
                                   Margin="0,0,0,4">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value=""/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedEvent.Succeeded}" Value="True">
                                            <Setter Property="Text" Value="成功"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding SelectedEvent.Succeeded}" Value="False">
                                            <Setter Property="Text" Value="失败"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Border Grid.Row="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8">
                                <TextBlock Text="{Binding SelectedEvent.Detail}" 
                                           FontSize="12"
                                           Foreground="{DynamicResource TextPrimary}"
                                           TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
