# 天命 macOS 功能对齐矩阵

日期：2026-05-10

状态说明：

- 复用：核心逻辑可迁入跨平台类库。
- 迁移：需要从 WPF/XAML 或 Windows API 改到 Avalonia/macOS 实现。
- 替换：原实现不可跨平台，需要平台等价方案。
- 降级：macOS v1 可保留核心能力，但部分系统级行为不同。
- 待决策：依赖服务端、账号体系、证书、授权策略或产品范围。

## 核心写作闭环

| 功能 | 原版位置 | macOS 处理 | 状态 |
|---|---|---|---|
| 项目管理 | `Framework/UI/Workspace/Services/ProjectManager.cs` | 项目配置读写、默认项目初始化、单书约束、重命名、切换、删除已端口到 `FileProjectManager`；UI 选择器和切换后的缓存/向量索引刷新仍需迁移 | 迁移 |
| 世界观规则 | `Modules/Design/GlobalSettings/WorldRules` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 角色规则 | `Modules/Design/Elements/CharacterRules` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 势力规则 | `Modules/Design/Elements/FactionRules` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 地点规则 | `Modules/Design/Elements/LocationRules` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 剧情规则 | `Modules/Design/Elements/PlotRules` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 创意素材库 | `Modules/Design/Templates/CreativeMaterials` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 智能拆书 | `Modules/Design/SmartParsing/BookAnalysis` | 解析服务复用，WebView2 爬虫替换 | 替换 |
| 大纲 | `Modules/Generate/GlobalSettings/Outline` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 分卷设计 | `Modules/Generate/Elements/VolumeDesign` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 章节规划 | `Modules/Generate/Elements/Chapter` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 蓝图 | `Modules/Generate/Elements/Blueprint` | 通用 categories/data JSON 读写、ID 补齐、分类级联删除已端口到 `FileModuleDataStore`；具体页面和表单仍需迁移 | 迁移 |
| 章节生成 | `Services/Framework/AI/SemanticKernel/Plugins/WriterPlugin.cs` | 业务逻辑复用，UI 调度隔离 | 迁移 |
| 生成门禁 | `Services/Modules/ProjectData/Implementations/Generation/GenerationGate.cs` | CHANGES 协议解析、门禁编排器、`GateResult`/`DesignElementNames`、正文实体/描述校验已端口到 `src/Tianming.ProjectData`；落盘前内容准备已端口到 `ContentGenerationPreparer`；最小生成闭环已端口到 `ChapterGenerationPipeline`；追踪更新调用已抽象为 `ChapterTrackingDispatcher`；Guide JSON 落盘已端口到 `FileChapterTrackingSink`；关键词索引已端口到 `FileChapterKeywordIndex`；生成后派生索引钩子已抽象为 `IChapterDerivedIndex` | 复用 |
| 章节文件存储 | `GeneratedContentService.cs` / `ContentGenerationCallback.cs` | Markdown 文件读写、排序元数据、安全 staging/backup 写入、卷存在检测、下一章/跨卷续写 ID 生成已端口到 `ChapterContentStore`；Guide 写入与章节移除回算已端口到 `FileChapterTrackingSink`；章节重写/删除级联已端口到 `ChapterGenerationPipeline`；关键词索引更新/清理已端口；向量索引更新/重写缓存失效已通过 `FileVectorChapterIndexAdapter` 接入派生索引钩子 | 复用 |
| 模块版本追踪 | `Services/Modules/VersionTracking` | `version_registry.json` 读写、模块版本自增、依赖快照、过期依赖检测、下游影响队列/flush 已端口到 `FileVersionTrackingService`；UI Toast 呈现留给 macOS 前端注入 | 复用 |
| 事实快照 / 账本一致性 | `Services/Modules/ProjectData/Implementations/Tracking` | `FactSnapshot`、`LedgerRuleSet`、`LedgerRuleSetProvider`、`LedgerConsistencyChecker` 已端口到 `src/Tianming.ProjectData/Tracking`；`PortableFactSnapshotExtractor` 已通过 `IFactSnapshotGuideSource` 端口普通章节上一章状态抽取、未来状态隔离、卷末全量最新状态抽取、角色/地点描述、世界观硬规则抽取、PlotPoints 相关情节点搜索、近期活跃实体补入和原版注入限额（活跃实体/势力/物品/时间线/PlotPoints）；`FileFactSnapshotGuideSource` 已按原版 guide/design JSON 文件布局接入跨平台文件读取与分卷聚合 | 复用 |
| 统一校验 | `Services/Modules/ProjectData/Implementations/Validation` | 正文实体/描述校验、门禁规则层校验已端口；AI 校验协议 JSON 模板、规则说明、规则签名与校验类型标签已端口到 `ChapterValidationPromptTemplate`；章节校验 prompt 文本骨架、章节信息、上下文 section、缺失规则说明、正文预览、结构性问题提示和返回格式要求已端口到 `ChapterValidationPromptComposer`；模板/世界观/角色/势力/地点/剧情规则的设计侧 prompt 上下文 ID 匹配、格式化、数量限制、Leader 名称映射与截断已端口到 `ChapterValidationDesignContextResolver`；大纲/章节规划/蓝图/分卷设计的生成侧 prompt 上下文 ID 匹配、格式化与截断已端口到 `ChapterValidationPromptContextResolver`；原版 `ValidationContext` 到 AI prompt sources 的映射已端口到 `ValidationContextPromptSourceMapper`；单章 `ValidationContext` / `ContextIdCollection` 加载到 AI prompt/executor 的编排已端口到 `ValidationContextAIWorkflow`；单章 AI 校验 prompt 输入拼装已端口到 `ChapterValidationPromptInputBuilder`，可合并章节元数据、设计侧上下文、生成侧上下文和已知结构性问题；AI 校验上下文源到 prompt/executor 的接线已端口到 `ChapterValidationAIWorkflow`；单章校验控制流已端口到 `ChapterValidationProcessor`，覆盖章节 ID 解析、缺正文错误、规则层到 AI 层委托顺序和总状态判定；批量章节校验 prompt 文本骨架、章节内联上下文、正文预览和 JSON 数组输出契约已端口到 `BatchChapterValidationPromptComposer`；批量章节内联角色/势力/剧情上下文 ID 匹配、数量限制和剧情目标截断已端口到 `BatchChapterValidationPromptContextResolver`；AI 校验协议 JSON 到 `ChapterValidationResult.IssuesByModule` 的解析、协议错误记录、缺失/未知模块处理与总状态判定已端口到 `ChapterValidationResultParser`；单章 AI 校验执行控制流（prompt 构建注入、AI 调用注入、成功解析、失败/异常降级、结构性问题传递）已端口到 `ChapterAIValidationExecutor`；批量 AI 校验数组按章节顺序写回、长度不足、缺 `moduleResults` 和解析失败降级已端口到 `BatchChapterValidationResultParser`；缺正文、单章验证、多章 AI 解析和 AI 空结果 fallback 的批次处理控制流已端口到 `ChapterValidationBatchProcessor`；卷级章节抽样数量与均匀抽样算法已端口到 `VolumeValidationChapterSampler`；卷级抽样、批次校验、批次异常降级、结果排序和卷级汇总组装已端口到 `VolumeValidationProcessor`；多个章节校验结果到卷级 `ValidationSummaryData` 的模块聚合、问题项、采样章节、图标和依赖版本映射已端口到 `VolumeValidationSummaryBuilder`；统一校验结果到 `ValidationReport` 的报告项、总状态、摘要和异常报告映射已端口到 `ValidationReportBuilder`；校验报告文件保存/读取/最新状态/删除已端口到 `FileValidationReportStore`；卷级校验汇总加载、按卷覆盖保存、分类绑定、删除和卷号解析已端口到 `FileValidationSummaryStore`；文件报告/汇总落盘接线已端口到 `FileValidationPersistenceAdapter`；`IUnifiedValidationService` 的跨平台包装层已端口到 `PortableUnifiedValidationService`，负责章节报告保存、卷级汇总保存和 NeedsRepublish 委托；完整 UI/API 接线仍需迁移 | 复用 |
| 一致性调和 | `ConsistencyReconciler.cs` | staging 章节恢复、章节 `.bak` 恢复/清理、config/modules tmp 清理、孤立 `_backup_*` 目录清理、guide JSON 损坏检测和有效 `.bak` 恢复、孤立摘要清理、缺失摘要从章节头部补建、卷里程碑重建回调、关键词索引缺失章节 best-effort 补建、`vector_degraded.flag` 自愈清理/保留、追踪缺口检测、孤立追踪清理、追踪缺口摘要补建、已完成卷 fact archive 回补已端口到 `PortableConsistencyReconciler` / `IChapterSummaryRepairStore` / `IChapterKeywordIndexRepairStore` / `IChapterVectorIndexRepairStore` / `IChapterTrackingRepairStore` / `IVolumeFactArchiveRepairStore`；快照抽取器文件接线已由 `FileFactSnapshotGuideSource` 平台化 | 复用 |
| 章节修复 | `Modules/Validate/ValidationSummary/ValidationResult` | 校验汇总问题展平与按章节 hints 构建已端口到 `ChapterRepairProblemHintBuilder`，覆盖可修复模块过滤、坏 JSON 忽略、字段保留和同章节摘要去重；修复提示词构建已端口到 `ChapterRepairPromptComposer`，覆盖有效 CHANGES 剥离、非法协议保留原文、原文章节 8000 字截断和问题清单编号；`PortableChapterRepairService` 已端口可注入的修复编排边界，覆盖上下文加载、缺失 `FactSnapshot` 阻断、修复 hints 传递、保存前必须持有最近快照、下一章标题衔接提示；真实 `WriterPlugin` / `AutoRewriteEngine` 调用接线、修复弹窗和页面仍需迁移 | 迁移 |
| 打包发布 | `PublishService.cs` / `PackageHistoryService.cs` | 写作导航纯数据目录、模块启停 JSON 读写/统计、模块变更检测、默认模块映射、模块 JSON 聚合、启用过滤、scope 过滤、全量/单模块发布闭环编排、manifest 读写、发布状态、历史版本保留、版本启用差异、历史恢复、恢复覆盖生成正文确认预检与保护、清除打包/生成/向量产物、发布统计章节/中文字数/角色/地点计数已端口到 `WritingNavigationCatalog` / `FileModuleEnabledStore` / `FileChangeDetectionStore` / `PackageModuleMappingCatalog` / `FilePackageBuilder` / `FilePackagePublisher` / `FilePackageManifestStore` / `FilePackageStatisticsBuilder`；导出 UI、恢复确认弹窗、缓存刷新仍需迁移 | 迁移 |

## AI 与对话能力

| 功能 | 原版位置 | macOS 处理 | 状态 |
|---|---|---|---|
| 模型管理 | `Modules/AIAssistant/ModelIntegration/ModelManagement` | `categories.json` / `providers.json` / `models.json` 包装或数组格式加载、`ProviderModels/*.models.json` fallback、Provider/Model 查询已端口到 `FileAIConfigurationStore`；页面仍需迁移 | 迁移 |
| API Key 配置 | `ModelService.cs` / `AIService.cs` | 用户配置增删改查、单 active 配置、重复 Provider/Model 覆盖更新、配置持久化且 `ApiKey` 不落盘已端口到 `FileAIConfigurationStore`；`IApiKeySecretStore` 与 `MacOSKeychainApiKeySecretStore` 已提供 macOS Keychain 真值存取边界；Key 编辑 UI 与真实 Keychain 人工验收仍需迁移 | 迁移 |
| 多 Key 轮换 | `ApiKeyRotationService.cs` | 轮换选择、排除列表、限流临时禁用、鉴权/配额永久禁用、连续服务错误冷却、状态查询、`KeyStateChanged` 事件已端口到 `src/Tianming.AI`；`ConfiguredAITextGenerationService` 非流式/流式生成已可选接入轮换 key、成功上报和 429 后排除当前 key 重试；Key 编辑 UI 仍需迁移 | 迁移 |
| 用量统计 | `UsageStatisticsViewModel.cs` / `StatisticsService.cs` | API 调用记录、汇总统计、每日统计、按模型聚合、最近记录、文件持久化、过期裁剪、清空统计已端口到 `FileUsageStatisticsService`；`ConfiguredAITextGenerationService` 非流式/流式生成已可选回写成功/失败调用、耗时、token 与错误信息；页面仍需迁移 | 迁移 |
| 生成统计 | `GenerationStatisticsService.cs` / `GenerationResult.cs` | `GenerationResult`、无存储副作用的 `GenerationStatisticsRecorder`、显式路径 `generation_statistics.json` 持久化 recorder 已端口到 `src/Tianming.ProjectData/Generation`；页面仍需迁移 | 复用 |
| 提示词管理 | `PromptManagement` | `PromptCategory` / `PromptTemplateData` 原模型、用户模板文件存储、内置模板递归加载、同分类同名用户模板覆盖内置模板、内置模板不可改删、模板 CRUD 与变更事件已端口到 `FilePromptTemplateStore`；AI 生成提示词的输入校验、元提示词构建、AI 失败映射、JSON/纯文本返回解析、tags 解析已端口到 `PromptGenerationCoreService`；OpenAI-compatible HTTP 适配器与 active configuration 生成接线服务已可作为 `IPromptTextGenerator` 接入；页面和完整 AIService 接线仍需迁移 | 迁移 |
| 提示词版本测试 | `VersionTesting` | `TestVersionData` 原模型、`test_versions.json` 加载、按创建时间倒序、版本测试增删改查、测试输出/评分/状态/时间字段更新、清空与安全临时文件保存已端口到 `FilePromptVersionStore`；单版本 AI 测试执行、成功/失败状态写回、同输入双提示词对比执行已端口到 `PromptVersionTestRunner`；页面仍需迁移 | 迁移 |
| SK 对话服务 | `SKChatService.cs` | thinking/answer 标签流式分流、`<think>` / `<analysis>` / `<answer>` 解析、噪声行过滤、分析块标题拆分已端口到 `TagBasedThinkingStrategy` / `ThinkingBlockParser`；真实 SK 调用链、流式 UI 消息和调度隔离仍需迁移 | 迁移 |
| Agent / Plan / Edit 模式 | `ModeProfileRegistry.cs` / `SKConversationViewModel.cs` | Ask/Plan/Agent 模式配置目录、Ask/Plan/Agent streaming message mapper、payload target、raw content 显示策略、执行引擎需求、轻量 `ConversationMessage` / `ConversationRole`、计划步骤解析、中文数字解析、`@续写/@重写` 章节指令解析、单章任务合并、多章范围拆分、明确多章节计划保持、content guide 快速计划、计划生成/解析失败摘要、执行完成/失败摘要、`ExecutionEvent`、`ExecutionTraceCollector`、`ExecutionTraceSummary`、`ToolCallRecord`、`PlanStep`/`PlanPayload`、`TodoExecutionService` 顺序执行/重试/失败中断/取消运行态已端口到 `src/Tianming.AI`；真实 WriterPlugin 工具调用接线与对话 UI 仍需迁移 | 迁移 |
| 会话历史 | `SessionManager.cs` / `SessionHistoryDialog` | 会话索引、消息 JSON 存储、创建/删除/重命名、模式更新、当前会话回落、空会话清理已端口到 `FileSessionStore`；ChatHistory 重建和弹窗 UI 仍需迁移 | 迁移 |
| 对话压缩 | `ChatHistoryCompressionService.cs` / `SKChatService.cs` | 90% 上下文触发、rolling memory 生成、结构化记忆合并、最近轮次保留、摘要失败硬截断已端口到不依赖 SemanticKernel `ChatHistory` 的 `ChatHistoryCompressionService`；SKChatService 接线与 UI 提示仍需迁移 | 迁移 |
| 项目上下文引用 | `ReferenceDropdownViewModel.cs` / `ReferenceParser.cs` | `ReferenceCatalog` / `ReferenceParser` 已端口到 `src/Tianming.AI/SemanticKernel/References`，覆盖原下拉类型目录、候选项按名称/ID 过滤、`@续写/@重写/@仿写` token 构建、英文别名解析、英文/中文冒号和空格分隔解析、标点边界截断以及从后向前替换引用；`ReferenceExpansionService` 已端口章节引用展开编排，覆盖章节摘要 context block、标题 HTML 转义、topK 关键片段截断、缺章节 ID/缺上下文提示和仿写引用保留；候选数据加载、真实 Guide/向量搜索委托接线和 Avalonia 下拉控件仍需迁移 | 迁移 |
| 待办执行 | `TodoExecutionService.cs` | 顺序执行、运行事件、重试、不可重试失败中断、后台启动、拒绝重入、取消复位已端口到 `src/Tianming.AI/SemanticKernel/TodoExecutionService.cs`；Todo 面板控件和 WriterPlugin 真实执行接线仍需迁移 | 迁移 |
| ONNX 向量搜索 | `VectorSearchService.cs` | `ITextEmbedder` 边界、纯托管 `HashingTextEmbedder`、有 embedder 时的本地相似度搜索、无模型/无依赖时的 keyword fallback、章节 Markdown 分块、相关度排序、按章节取片段、删除后缓存失效已端口到 `FileVectorSearchService`；`FileVectorChapterIndexAdapter` 已可挂入章节保存/重写/删除流水线；ONNX 真向量、KernelMemory/OpenAI 混合模式、模型路径与 macOS OnnxRuntime 包仍需验证 | 迁移 |
| 兼容接口调用 | `AIService.cs` / `SKChatService.cs` | OpenAI-compatible HTTP 请求、`/v1/chat/completions` URL 规范化、Bearer / `X-Api-Key` / Cherry Studio 兼容头、非流式 message content/usage 解析、SSE `data:` delta content 流式解析、HTTP error payload 映射、模型名前缀与 `-free` 清理已端口到 `OpenAICompatibleChatClient`；`ConfiguredAITextGenerationService` 已把 active configuration、Provider endpoint、Model name、DeveloperMessage、ApiKey 和可选多 Key 轮换串到非流式/流式文本生成；模型名泄露过滤 `ModelNameSanitizer` 已端口到 `src/Tianming.AI`；SemanticKernel、Anthropic/Gemini/Azure 多协议接线和 UI 调度仍需迁移 | 迁移 |

## 编辑器与工作区

| 功能 | 原版位置 | macOS 处理 | 状态 |
|---|---|---|---|
| 主窗口 | `Core/App/MainWindow.xaml` | Avalonia Window + DI + 窗口状态恢复已就位 | 框架已就位（M3） |
| 三栏布局 | `Framework/UI/Windows/ThreeColumnLayout.xaml` | Avalonia Grid + GridSplitter 三栏 + 宽度持久化已就位 | 框架已就位（M3） |
| 左侧导航 | `Framework/UI/Workspace/LeftPanel` | LeftNavView + NavigationService + PageRegistry 已就位；M4 扩展模块条目 | 框架已就位（M3） |
| 中央编辑器 | `Framework/UI/Workspace/CenterPanel/ChapterEditor` | Markdown 编辑器重建或替换 | 替换 |
| 多标签页 | `EditorTabBar.xaml` / `EditorTabManager.cs` | 管理逻辑复用，UI 迁移 | 迁移 |
| 仪表盘 | `DashboardView.xaml` | 页面迁移 | 迁移 |
| 计划视图 | `PlanView.xaml` / `PlanViewModel.cs` | 逻辑复用，页面迁移 | 迁移 |
| 主页视图 | `HomepageView.xaml` | 页面迁移 | 迁移 |
| 内联编辑 | `InlineEditPopup` | 交互重建 | 迁移 |
| 右侧对话面板 | `ConversationPanel.xaml` | Avalonia 对话流重建 | 迁移 |
| Markdown 渲染 | `Markdig.Wpf` | 改为 Avalonia Markdown 渲染或自定义渲染 | 替换 |
| Diff 查看 | `DiffPlex.Wpf` | 保留 DiffPlex 核心，替换 WPF 控件 | 替换 |

## 框架能力

| 功能 | 原版位置 | macOS 处理 | 状态 |
|---|---|---|---|
| 主题管理 | `Framework/Appearance/ThemeManagement` | `PortableThemeType` / `PortableTimeBasedThemeSettings` / `PortableThemeApplicationPlanner` / `PortableThemeResourcePalette` / `PortableThemeFilePaletteLoader` / `PortableThemeStateController` 已端口主题枚举、定时主题配置数据、原版主题文件名/显示名/预览色映射、关键 brush key palette、Auto 浅深解析、Custom 文件名规范化、自定义主题 XAML 关键 brush 解析/校验、当前主题状态持久化、内置/自定义主题切换控制、无效自定义主题失败结果和变更事件；`FileThemeSelectionStore` 已端口主题收藏、最近使用、搜索历史、筛选偏好持久化、去重/裁剪和坏 JSON 恢复；`PortableThemeImportExportService` 已端口主题文件验证、导入覆盖策略、当前主题导出、批量导出和导出列表读取；`PortableThemeDesigner` 已端口主题 XAML 生成、主题文件名清洗、原子保存和 WCAG 对比度警告；ThemeBridge 已桥接 PortableThemeStateController → Avalonia FluentTheme 变体切换 + 动态 brush 注入（M3）；主题设计器/选择页面和真实资源系统仍需迁移 | 框架已就位（M3） |
| 自动主题冲突解析 | `Framework/Appearance/AutoTheme/ConflictResolver.cs` | `PortableThemeConflictResolver` 已端口系统跟随请求、定时主题请求、当前主题 fallback、优先级高者胜出、Resolve 同优先级定时切换优先、DetectConflict 同优先级跟随系统展示胜出、冲突描述和 resolved theme；真实主题应用调度与 UI 提示仍需迁移 | 复用 |
| 主题过渡动画 | `Framework/Appearance/Animation/ThemeTransition` | `PortableThemeTransitionSettings` / `PortableEasingFunctions` / `FileThemeTransitionSettingsStore` 已端口原版过渡枚举、默认设置、clone、线性/二次/三次/弹性/弹跳缓动函数、曲线采样、Fast/Smooth/Fancy/Simple/Dynamic/Cool 预设、FPS/时长/强度 clamp、组合效果去重过滤和设置 JSON 原子保存/坏 JSON 恢复；真实 Avalonia renderer、主题切换触发接线、显示器刷新率探针和设置页面仍需迁移 | 迁移 |
| 主题跟随系统 | `SystemThemeMonitor.cs` / `SceneDetector.cs` | `PortableSystemFollowSettings` / `PortableSystemFollowPolicy` / `PortableSystemFollowController` / `PortableSystemFollowRuntime` / `PortableSystemFollowStatisticsAnalyzer` / `PortableSystemFollowApplicationSceneClassifier` / `MacOSSystemAppearanceParser` / `MacOSSystemAppearanceProbe` / `MacOSSystemAppearanceMonitor` / `MacOSRunningApplicationProbe` 已端口原版系统跟随默认设置、浅/深/高对比度主题映射、排除时间段同日/跨日匹配、场景禁用切换、最小切换间隔抑制、外观 monitor 到 controller 运行态接线、auto-start、启停保存、事件退订、外观变化到主题切换 controller、成功切换保存与通知边界、切换失败降级保存、macOS `AppleInterfaceStyle` 输出解析、`/usr/bin/defaults` 命令探针、失败回退、macOS 外观轮询监听、启动基线捕获、变更事件和停止取消、主题变化历史最新优先/20 条裁剪/显示文本、运行应用场景分类、macOS osascript 运行应用探针、每日/每周/每月切换统计、7 日主题使用占比、24 小时峰值时段、平均/最近切换耗时、最常用主题、性能评级、30 天记录裁剪、清空统计和设置 JSON 原子保存/坏 JSON 恢复；NSDistributedNotification 原生 hook、Avalonia 主题资源适配、通知呈现和设置页面仍需迁移 | 替换 |
| 定时主题 | `TimeScheduleService.cs` / `HolidayLibrary.cs` / `SunCalculator.cs` | `PortableThemeScheduleService` / `PortableTimeScheduleController` / `PortableTimeScheduleTimerService` / `PortableHolidayLibrary` / `PortableSunCalculator` 已端口简单日/夜主题、跨午夜时间窗、灵活时间段优先级、星期匹配、2024/2025 内置节假日库、防御性查询、按年/月筛选、节假日跳过/覆盖、临时禁用到期和日出日落算法与 fallback、settings.json 原子保存/坏 JSON 恢复、切换成功/失败历史记录和 100 条裁剪、启动即检查、主题不同才切换、临时禁用到期恢复、切换失败降级记录、1 分钟周期调度、重复启动幂等和停止取消；macOS/Avalonia 启动接线、设置页面和主题应用适配仍需迁移 | 迁移 |
| 字体管理 | `Framework/Appearance/Font` | `PortableFontCatalog` / `FileFontUsageStore` 已端口字体分类、主题字体推荐、标签生成、字体收藏、最近使用计数/排序/20 条裁剪和坏 JSON 恢复；`FileFontPresetStore` 已端口内置预设、自定义预设保存/覆盖、删除、导入去重、导出和坏 JSON 恢复；`FileFontConfigurationStore` 已端口 UI/编辑器字体配置默认值、保存、局部更新、导入/导出和 share package；`PortableEditorFontPresetService` 已端口原版 9 个编辑器字体内置预设、下载地址、特性标签、安装状态注入和大小写无关查询；`PortableCodeSampleProvider` 已端口原版 14 个语言枚举、C#/Python/JavaScript/TypeScript/JSON/Markdown 六类实际样例和无样例语言回退 C#；`PortableFontInfoService` 已端口字体元数据探针边界、字形数、拉丁/西里尔/中日韩/阿拉伯/符号脚本检测、缓存清理、变体探针和异常降级；`MacOSFontCatalog` 已端口 macOS 字体目录扫描、字体名规范化、`.ttf`/`.ttc`/`.otf` 扩展过滤、大小写去重和缺失目录跳过；`MacOSFontImporter` 已端口用户字体目录导入、目标目录创建、同名文件编号避让、导入字体名回报、缺失源文件拒绝和不支持扩展拒绝；`PortableFontResourceMapper` 已端口 UI/编辑器字体资源键、比例字号、字重 fallback 和渲染模式映射；`FileFontFallbackService` / `PortableFontFallbackService` 已端口默认回退链、回退链 JSON 持久化、添加/移除回退字体、字体族字符串构建、等宽/普通字体推荐和坏 JSON 恢复；`PortableLigatureDetector` 已端口已知编程连字字体识别、通用连字列表、预览文本分组和缓存边界；`PortableMonospaceFontDetector` 已端口原版 known monospace 字体名单、5% 字符宽度偏差判定、可注入测量边界和缓存清理；`PortableCharWidthAnalyzer` 已端口数字/字母/符号/CJK 宽度一致性报告、原版阈值、总状态与 summary 文案；`PortableFontPerformanceAnalyzer` 已端口四类文本样本性能测量聚合、Excellent/Good/Fair/Poor 阈值、summary 文案和失败降级；`PortableOpenTypeFeaturesService` 已端口已知字体 OpenType 特性表、默认 liga/calt fallback、大小写无关查询、特性说明和未知特性 fallback；`PortableScenePresetService` 已端口编码/阅读/演示/终端/技术文档五类场景预设、原版字体设置、推荐字体和提示文案；Avalonia renderer 接线和字体页面仍需迁移 | 迁移 |
| 图片取色 | `ImageColorPicker` | `PortableImageColorAnalyzer` 已端口 BGRA32 像素读取、stride padding 处理、最大边长采样、调色板提取、极亮/极暗像素过滤、颜色量化与多样性筛选、亮度/暗色比例/亮色比例分析、深浅主题和文字色推荐、主色到最远辅助色选择、图片主题色生成和 `PortableThemeDesignSnapshot` 映射；Avalonia/macOS bitmap 解码适配、真实图片文件加载、主题文件保存/应用接线和页面仍需迁移 | 替换 |
| AI 配色生成 | `AIColorSchemeViewModel.cs` / `AIColorSchemeSettings.cs` | `PortableAIColorSchemeCore` 已端口 AI prompt 参数约束、深/浅主题背景与文字色约束、JSON 数组提取与容错解析、缺名默认、坏色值灰色 fallback、对比度/主色对比/饱和度评分和 `PortableThemeDesignSnapshot` 映射；`FileAIColorSchemeSettingsStore` 已端口 `ai_color_scheme_data.json` 用户配置默认值、保存、覆盖更新、原子写入、坏 JSON/缺配置恢复和选项值规范化；真实 AI 调用、取消状态、历史写入接线、主题文件保存/应用接线和页面仍需迁移 | 迁移 |
| 智能配色生成历史 | `GenerationHistorySettings.cs` | `FileGenerationHistoryStore` 已端口 `generation_history.json` 读写、ID 补齐、同 ID 覆盖、最新优先排序、收藏、按类型/日期范围/关键词筛选、删除、清空、坏 JSON 恢复，以及总数/收藏/图片取色/AI 配色/今日/本周/本月统计；历史记录页面、应用/导出命令和 UI 确认仍需迁移 | 迁移 |
| UI 分辨率与缩放 | `UIResolutionSettings.cs` / `UIResolutionService.cs` | `PortableUIResolutionSettings` / `FileUIResolutionSettingsStore` 已端口 720p/1080p/1440p/自定义预设、缩放显示名、默认窗口尺寸、最小/最大尺寸校验、clone、`settings.json` 原子保存、读取和坏 JSON 恢复；真实 Avalonia 窗口尺寸应用、跨窗口缩放、屏幕分辨率探针和设置页面仍需迁移 | 迁移 |
| 加载动画 | `LoadingAnimationSettings.cs` / `LoadingAnimationService.cs` | `PortableLoadingAnimationSettings` / `FileLoadingAnimationSettingsStore` 已端口动画类型、位置、遮罩模式、速度、尺寸、颜色、不透明度、文本、百分比、遮罩参数、最小展示时间、延迟、点击取消、声音配置、clone、`settings.json` 原子保存、读取、父目录创建和坏 JSON 恢复；真实 Avalonia overlay、动画渲染、点击取消、声音播放和设置页面仍需迁移 | 迁移 |
| 应用内 Toast | `ToastNotification.xaml` | `PortableNotificationDispatcher` 已抽出 Toast 投递前的跨平台核心链路，覆盖勿扰拦截、历史记录、声音播放开关和系统通知 sink 调用；`PortableToastPresenter` / `PortableToastPlacementCalculator` / `FileToastStyleSettingsStore` / `FileToastTypeSettingsStore` 已端口原版静态入口默认 3000ms、空消息隐藏、默认样式数据、simple/standard/fancy 样式预设、样式 JSON 原子保存/坏 JSON 恢复、通知类型原版 6 项默认元数据、类型配置 JSON 原子保存/坏 JSON 恢复、类型配置优先与禁用 fallback、BottomRight 等屏幕定位/堆叠公式；Avalonia Toast 控件、动画执行、通知样式页面、通知类型页面和真实前端接线仍需重建 | 迁移 |
| 系统通知 | `WindowsNotificationService.cs` | `PortableNotificationDispatcher` 已端口系统通知投递接口边界，覆盖启用/禁用系统通知时的 Delivered/RecordedOnly/Blocked 状态；`MacOSNotificationSink` 已端口 `/usr/bin/osascript` `display notification` 命令边界、标题/正文转义、通知类型 subtitle 和失败错误传播；Notification Center 权限人工验收、Avalonia Toast 接线和设置页面仍需迁移 | 迁移 |
| 系统集成 | `SystemIntegrationSettings.cs` / `SystemIntegrationView.xaml.cs` | `PortableSystemIntegrationSettings` / `FileSystemIntegrationSettingsStore` / `PortableSystemIntegrationController` 已端口原版 `system_integration.json` 默认值、原版 JSON 键兼容、原子保存/坏 JSON 恢复、通知启用/声音/优先级更新、重置默认和 macOS apply plan；LaunchAgent/SMAppService、Info.plist URL 协议、UTI/Document Types 标记为需平台接线，Windows 右键菜单与发送到菜单标记为 macOS 不适用；真实 macOS 系统集成执行、菜单栏图标和页面仍需迁移 | 迁移 |
| 通知历史 | `NotificationHistory` | `FileNotificationHistoryStore` / `PortableNotificationHistoryController` 已端口到 `src/Tianming.Framework/Notifications`，覆盖 `notification_history.json` 读写、最新记录前置、最多 10 条裁剪、已读标记、删除、清空、坏 JSON 恢复、blocked 标记保留、可绑定记录快照、总数/未读统计、原版时间显示、删除/清空成功文案和清空确认边界；`PortableNotificationDispatcher` 已接入历史写入；通知历史页面仍需迁移 | 迁移 |
| 勿扰模式 | `DoNotDisturb` | `DoNotDisturbSettingsData` / `DoNotDisturbPolicy` / `FileDoNotDisturbSettingsStore` / `PortableDoNotDisturbController` / `MacOSFullScreenApplicationDetector` 已端口到 `src/Tianming.Framework/Notifications`，覆盖原版默认值、启用开关、紧急通知例外、跨日/同日时间窗、例外应用匹配、`AutoEnableInFullscreen` 自动启用拦截、`dnd_settings.json` 原子保存/坏 JSON 恢复、切换状态、快捷启用和状态文案/颜色、macOS 前台应用 `AXFullScreen` 状态解析和 `/usr/bin/osascript` 命令边界；`PortableNotificationDispatcher` 已接入勿扰拦截；设置页面和真实通知投递接线仍需迁移 | 迁移 |
| 声音方案 | `Framework/Notifications/Sound` | `PortableNotificationSoundPlayer` 已端口通知类型到事件名映射、自定义音效优先、缺失文件系统音 fallback、通知音量归一化、静音和“无”映射跳过；`FileSoundSchemeSettingsStore` / `PortableSoundSchemeController` 已端口声音方案 `settings.json` 默认值、原子保存、坏 JSON/缺文件恢复、原版通知/警告/错误/成功/信息提示事件映射，以及 default/silent/minimal/rich 内置方案切换；`PortableSoundLibrary` 已端口音效库 `.wav/.mp3` 文件枚举、大小/分类元数据、导入复制、重名避让、仅库内删除和 macOS `/usr/bin/open` 打开目录命令计划；`FileVolumeAndDeviceSettingsStore` / `PortableVolumeAndDeviceController` / `PortableEqualizerController` / `PortableAudioDeviceSelectionController` / `PortableAudioDeviceTestController` 已端口音量与设备 `settings.json` 原模型字段、原子保存、坏 JSON 恢复、安静/标准/响亮三档预设、静音 toggle、原版 9 项均衡器预设、手动五段增益 -12..12 clamp、输出/输入设备选择写回、缺失设备拒绝、输入输出类型防错、输出测试音和输入设备就绪校验；`MacOSNotificationSoundOutput` 已端口 `/usr/bin/afplay` 音频播放命令边界、自定义文件播放、portable system sound 到 macOS `.aiff` 的映射、音量 clamp 和失败错误传播；`MacOSAudioDeviceCatalog` 已端口 `/usr/sbin/system_profiler SPAudioDataType` 音频设备枚举、输入/输出分类、默认设备识别、Transport 解析和失败错误传播；`MacOSSystemVolumeController` 已端口 `/usr/bin/osascript` 系统主音量读取、0-100 clamp 写入、静音读取/设置、ToggleMute 和固定音量范围；声音方案页面、音效库页面、音频设置页面、真实默认设备切换、实时 EQ 音频处理和真实 UI 接线仍需迁移 | 迁移 |
| 语音播报 | `System.Speech` | `PortableNotificationSoundPlayer` 已抽象 `IPortableSpeechOutput` 并端口播报开关、按通知类型播报过滤、标题/内容文本拼接、速度/音量/音调参数传递；`FileVoiceBroadcastSettingsStore` / `PortableVoiceBroadcastController` 已端口语音播报 `settings.json` 默认值、原子保存、坏 JSON/缺文件恢复、速度/音量/音调 clamp、测试文本 fallback 和按通知类型过滤；`MacOSSpeechOutput` 已端口 `/usr/bin/say` 语音播报命令边界、可选 voice 参数、速度到 macOS rate 的有界映射、空文本跳过和失败错误传播；`MacOSSpeechVoiceCatalog` 已端口 `say -v ?` voice 列表命令和输出解析；voice UI 绑定、真实 voice 选择接线和页面仍需迁移 | 迁移 |
| 代理配置 | `Framework/SystemSettings/Proxy` | `PortableProxyRouter` 已端口到 `src/Tianming.Framework/Proxy`，覆盖 HTTP/HTTPS 应用内代理 URI、认证凭据、Domain/IP/Wildcard/Regex 规则优先级、Direct/Proxy/Block 动作、`<local>`/通配符/域名旁路列表和 SOCKS 协议对托管 HTTP 客户端的安全降级；`PortableProxyTestHistoryStore` / `PortableProxyVerificationEvaluator` / `PortableProxyNetworkProbe` 已端口测试历史最新优先/上限裁剪/坏 JSON 恢复、一键验证 direct/proxy IP 判定、匿名分计算、连接延迟、出口 IP/位置解析、下载测速、DNS 探测失败降级和可注入真实 HTTP client/proxy adapter；`PortableProxyChainSelector` / `PortableProxyChainAnalyzer` / `PortableProxyChainHealthController` 已端口 active chain、enabled/available 节点筛选、无可用节点回退、LoadBalance 低延迟优先选择、历史汇总性能分析、成功率/延迟评级、链路对比、优化建议、报告 top/poor performers 和健康分、代理链健康检查、节点可用性与延迟刷新、history 记录、禁用自动 failover 时跳过检查，以及当前链不可用时切换到首个可用 enabled 链；`MacOSSystemProxyConfigurator` 已端口 `/usr/sbin/networksetup` 系统 HTTP/HTTPS/SOCKS 代理、PAC URL/启用状态、旁路域名和禁用命令边界；Avalonia 页面、外部测速/IP 服务人工验收和 `networksetup` 真实系统验收仍需迁移 | 迁移 |
| 日志系统 | `Services/Framework/Settings/LogManager.cs` / `Framework/SystemSettings/Logging` | `PortableLogService` 已端口到 `src/Tianming.Framework/Logging`，覆盖模块前缀解析、日志级别推断、全局/最小/模块阈值过滤、timestamp/level/message/caller/thread/process/exception 格式化、短级别名称、相对日志目录解析、按 `{date}_{appname}.log` 写入；`PortableLogMaintenance` 已端口 `.log` 文件清理策略，覆盖启停、按数量保留、按天数保留和相对目录解析；`PortableLogFormatCore` 已端口 LogFormat 预览、Text/JSON/XML 输出示例、内置字段/自定义字段校验、未知字段/大括号不匹配/字段过多警告；`PortableLogLevelCore` 已端口 Development/Testing/Production 预设、级别变更记录、原版级别颜色和统计百分比；`PortableLogRotationCore` 已端口存储空间状态、轮转预测、过期日志/压缩旧日志/数量超限/紧急清理建议；`PortableLogOutputPipeline` 已端口异步高/低优先级队列、低优先级丢弃汇报、按目标优先级分发、多输出目标失败隔离和失败记录边界；`PortableFileLogOutputSink` / `PortableTextWriterLogOutputSink` / `PortableHttpLogOutputSink` / `PortableTcpLogOutputSink` / `MacOSLoggerLogOutputSink` 已端口 file/console/HTTP/TCP/event 输出 sink、相对路径 storage root 解析、目录创建、追加写入、TextWriter 注入、HTTP POST、TCP UTF-8 写入和 macOS `/usr/bin/logger` 事件日志命令边界；`PortableLogOutputTelemetryStore` 已端口输出统计滚动均值、成功率、失败记录最新优先/100 条裁剪、JSON 原子保存、坏 JSON 恢复和清空；`FileLogOutputSettingsStore` / `PortableLogOutputTargetBuilder` / `PortableLogOutputTestRunner` / `PortableLogOutputSettingsController` 已端口原版日志输出设置默认值、file/console/event/remote 开关、retry 配置、显式输出目标列表、settings.json 原子保存/坏 JSON 恢复、pipeline target 映射、测试所有输出目标、成功/失败结果、测试统计/失败记录回写、添加/删除目标、保存设置和清空 telemetry；Avalonia 日志格式/级别/轮转/输出设置页面仍需迁移 | 迁移 |
| 数据清理 | `DataCleanupViewModel.cs` | `PortableDataCleanupService` / `PortableDataCleanupController` 已端口到 `src/Tianming.Framework/Cleanup`，覆盖清理项扫描统计、JSON 内容清空、文件删除、目录文件清理、内置模板/分类保护、自定义模板仅保留内置项、模型分类仅保留一级分类、storage root 路径逃逸保护，以及项目级卷/章节文件、备份/暂存稿、tracking guides、fact archives、milestones、plot points、summaries、keyword index、vector index 联动清理、项目 History 版本目录清理和清理后 AIService/SessionManager 刷新规划与注入式执行；Avalonia 页面仍需迁移 | 迁移 |
| 系统信息 | `SystemInfoViewModel.cs` | `PortableSystemInfoService` 已端口到 `src/Tianming.Framework/SystemInfo`，覆盖 OS/CPU/内存/磁盘/网络快照、字节/带宽格式化、原版核心文本报告导出结构，并以可注入探针隔离平台采集；`PortableSystemInfoReportExportPlanner` / `PortableSystemInfoReportExporter` 已端口原版默认导出路径命名和报告原子写盘；`MacOSSystemProfilerParser` / `MacOSSystemProfilerCatalog` 已端口 `/usr/sbin/system_profiler` 的 `SPHardwareDataType` / `SPDisplaysDataType` 命令边界，覆盖 macOS 型号/型号标识/芯片/内存/固件版本、GPU 芯片/核心/Metal 支持和显示器分辨率/主显示器/在线状态/连接类型解析，并可注入 `CollectSnapshot()` 和报告输出；系统信息页面仍需迁移 | 迁移 |
| 运行环境 | `RuntimeEnvViewModel.cs` | `PortableRuntimeEnvironmentService` 已端口运行时版本、CLR 版本、runtime identifier、GC 模式、区域、时区、程序集列表、环境变量列表、系统程序集/用户程序集过滤、环境变量关键词过滤、完整路径/文件名切换和原版运行环境报告文本；`PortableRuntimeEnvironmentReportExportPlanner` / `PortableRuntimeEnvironmentReportExporter` 已端口原版默认导出路径命名和报告原子写盘；RuntimeEnv 页面仍需迁移 | 迁移 |
| 诊断信息 | `DiagnosticInfoViewModel.cs` | `PortableDiagnosticInfoService` 已端口实时诊断快照模型、磁盘空间阈值检查、内存/Gen2 GC/线程数建议、健康/良好/需要关注状态与颜色、系统建议列表和原版诊断报告文本结构；`DefaultPortableDiagnosticInfoProbe` 已端口真实进程/GC/磁盘/运行上下文采集边界；`PortableDiagnosticReportExportPlanner` / `PortableDiagnosticReportExporter` 已端口原版默认导出路径命名和报告原子写盘；`PortableDiagnosticInfoController` 已端口自动刷新计划、刷新命令、健康检查命令和默认路径导出命令控制流；Avalonia timer host 和页面绑定仍需迁移 | 迁移 |
| 系统监控 | `SystemMonitorViewModel.cs` | `PortableSystemMonitorService` 已端口到 `src/Tianming.Framework/SystemMonitor`，覆盖 CPU 频率/占用/温度格式化、内存可用量与占用率、磁盘使用率、网络流量与速率、传感器不可用 fallback，并以可注入探针隔离 WMI/PerformanceCounter；`FileSystemMonitorSettingsStore` / `PortableSystemMonitorController` 已端口原版 `settings.json` 的 `LastRefreshTime`、原子保存、坏 JSON 恢复和刷新命令保存最后刷新时间控制流；`MacOSSystemMonitorParser` / `MacOSSystemMonitorProbe` 已端口 macOS `top -l 1 -n 0` CPU 使用率、`pmset -g batt` 电池电量和 `powermetrics --samplers smc -n 1` CPU/GPU/Ambient 温度、多风扇转速命令边界与解析，并在 `powermetrics` 权限不足/执行失败时回填可展示权限传感器；系统监控页面、powermetrics 真实硬件验收和更多真实硬件传感器目录仍需迁移 | 迁移 |
| 显示偏好 | `Framework/User/Preferences/Display` | `PortableDisplaySettings` / `FileDisplaySettingsStore` / `PortableDisplayController` 已端口原版 `display_settings.json` 默认字段、功能栏显示开关、列表密度、UI scale 80%-200% clamp、原子保存/坏 JSON 恢复和可绑定 apply plan；Avalonia 设置页面和真实窗口缩放接线仍需迁移 | 迁移 |
| 语言区域偏好 | `Framework/User/Preferences/Locale` | `PortableLocaleSettings` / `FileLocaleSettingsStore` / `PortableLocaleController` 已端口原版 `locale_settings.json` 默认字段、语言/时区/日期/数字格式更新、原子保存/坏 JSON 恢复、数字/日期预览和 macOS 下 Windows `China Standard Time` 到 `Asia/Shanghai` 的 apply plan；Avalonia 设置页面、真实重启流程和更多语言包资源仍需迁移 | 迁移 |

## 用户与服务端能力

| 功能 | 原版位置 | macOS 处理 | 状态 |
|---|---|---|---|
| 用户资料 | `Framework/User/Profile/BasicInfo` | `PortableUserProfileData` / `FileUserProfileStore` / `PortableUserProfileService` / `PortableUserProfileSyncController` 已端口原版 `user_profile.json` 字段、默认资料、Profiles 用户文件路径清洗、缺失资料创建、原子保存/坏 JSON 恢复、头像复制/读取/删除、资料导入导出、`/api/account/profile` GET/PUT 签名请求、服务端资料拉取覆盖本地、location 三段拼接/拆分、无 refresh token/待同步队列时跳过拉取、同步失败写入 `profile_pending_sync.json` 和成功同步清理 pending；BasicInfo/Avatar 上传页面与账号状态联动仍需迁移 | 迁移 |
| 密码锁定 / 密码安全 | `Framework/User/Security/PasswordProtection` / `Framework/User/Account/PasswordSecurity` | `PortableAppLockConfig` / `FileAppLockConfigStore` / `PortableAppLockPolicy` / `PortableAppLockController` 已端口原版 `app_lock_config.json` 字段、启动锁定/切换锁定/自动锁定策略、最后活动时间、失败次数阶梯锁定（3/5/10 次）、锁定状态、紧急解锁码 SHA-256 hash 与一次性消费；`FilePasswordSecurityStore` / `PortableAccountSecurityService` / `PortablePasswordStrengthPolicy` / `PortablePasswordChangeController` / `PortableTotp` 已端口 `password_hash.json`、`password_history.json`、`2fa_secret.json`、PBKDF2 初始密码设置/验证、旧 SHA256 hash 验证后升级、原版修改密码控制流、旧密码校验、服务端改密同步、网络错误本地化、历史密码重复拒绝、成功后清空输入、TOTP 启用/禁用控制流、TOTP URI 生成、密钥四位分组格式化、验证码空值/成功/失败提示和 TOTP 生成/邻近时间步校验；`PortableAccountLockoutData` / `FileAccountLockoutStore` / `PortableAccountLockoutController` / `PortableAccountLockoutServerController` 已端口 `lockout_data.json`、登录失败/成功记录、5/10/15 次账号锁定阈值、永久锁定、最近记录、7 日统计、`/api/account/lockout` 服务端状态同步、同步失败保留本地状态、`/api/account/unlock` 手动解锁和服务端失败时仍清理本地锁定；密码设置 UI、锁屏 overlay 和真实密码输入验证接线仍需迁移 | 迁移 |
| 登录窗口 | `Framework/User/Account/Login` / `ApiService.LoginAsync` / `AuthTokenManager` | 本地模式可绕过；`PortableAuthApiProtocol` 已端口登录 API 路径、登录请求 JSON 字段、`X-Client-Id`、User-Agent、Bearer header、统一响应 envelope 和 token/user 结果解析；`PortableAuthTokenStore` 已端口 clientId 生成/保留、登录/注册/刷新 token 持久化、清除登录态、旧 JSON 迁移和原版 HMAC 签名头生成；`MacOSKeychainAuthTokenProtector` 已端口 `/usr/bin/security` Keychain 写入/读取/删除命令和 token 文件引用边界；`PortableAuthApiClient` 已端口登录/注册/OAuth/订阅/账号绑定/登录历史/改密/登出真实 HTTP client 核心、成功后 token 保存、认证请求 Bearer+签名头、过期 token refresh 后业务请求重试和 `/api/auth/logout` 签名登出；`PortableAuthStartupController` 已端口启动登录态检查、有效 token 放行、过期 token refresh、无 refresh token 阻断、设备被踢清理 token/订阅缓存、异常降级和登出后本地状态清理；`PortableLoginController` 已端口原登录窗口控制流，覆盖账号/密码必填、账号 trim、登录成功回传用户名、记住账号/密码保存、关闭记住时清理、订阅未激活/过期 warning 展示、失败后清空密码和异常前缀；`PortableLoginHistoryStore` / `PortableLoginHistoryApiProtocol` 已端口本地 `login_history.json`、记录登录、风险评估、服务端历史 API path/query/envelope、服务端历史合并、日期/设备筛选、统计与安全评分、风险记录、24 小时活动会话、结束会话、清空、最新 200/合并 400 条裁剪和坏 JSON 恢复；`PortableAuthResultMessages` 已端口登录/注册输入校验、网络错误、用户名已存在、设备被踢、订阅未激活/过期、服务端 fallback 和异常前缀消息；Avalonia 登录窗口、DI 接线、登录历史页面和真实 Keychain 人工验收仍需迁移 | 迁移 |
| 注册 | `RegisterWindow.xaml.cs` / `ApiService.RegisterAsync` | `PortableAuthApiProtocol` 已端口注册 API 路径、用户名/密码/email/cardKey 请求字段和统一响应 envelope 解析；`PortableRegisterController` 已端口原注册窗口控制流，覆盖账号/密码/确认密码/卡密校验、文本修剪、成功后返回已注册账号密码、用户名已存在/网络错误等服务端错误映射和异常前缀；Avalonia 注册窗口、焦点绑定和真实服务端接线仍需迁移 | 迁移 |
| OAuth | `OAuthService.cs` / `ApiService.OAuthLoginAsync` | `PortableAuthApiProtocol` 已端口 `/api/auth/oauth/{platform}` 路径、platform/code/state 请求字段、免 Bearer 的 OAuth 登录请求和统一响应 envelope 解析；`PortableOAuthAuthorizationCore` 已端口默认平台授权 URL/scope、callback URL、授权 URL query 构建、微信 `appid` 特例、未配置/不支持平台提示、callback 成功/错误/state mismatch/缺 code 解析；`PortableOAuthProviderConfigStore` 已端口 `oauth_providers.json` provider 默认配置、ClientId 落盘、配置状态检测、自定义 URL/scope 覆盖、未知平台拒绝、坏 JSON 恢复和防御性拷贝；`PortableOAuthBrowserLauncher` 已端口 macOS `/usr/bin/open` 授权浏览器启动边界、HTTP/HTTPS URL 校验和打开失败错误映射；`PortableOAuthCallbackListener` 已端口本地 `/oauth/callback` 监听编排、非 callback 404、成功/错误/state mismatch HTML 响应、取消边界和默认 `HttpListener` request source；`PortableOAuthAuthorizationFlow` 已端口原版 `StartAuthorizationAsync` 控制流，覆盖准备授权 URL、启动 listener、打开浏览器、等待 callback、浏览器失败停止 listener 和取消/超时错误映射；`PortableOAuthLoginController` 已端口原登录窗口第三方登录按钮控制流，覆盖平台绑定检查、平台未配置 warning、授权失败 inline error、授权成功后 OAuth API 登录、成功回传用户名、服务端失败 fallback、异常前缀和平台显示名映射；平台授权页和真实服务端 OAuth 端点仍需迁移 | 迁移 |
| 账号绑定 | `AccountBinding` / `ApiService.GetBindingsAsync` / `ApiService.BindAccountAsync` / `ApiService.UnbindAccountAsync` | `PortableAccountBindingApiProtocol` 已端口绑定列表、绑定账号、解绑账号三条 API 路径、平台 path segment 转义、OAuth code/state 请求字段、Bearer 接入边界、统一响应 envelope 和绑定结果解析；`PortableAuthApiClient` 已接入绑定列表/绑定/解绑真实 HTTP client 核心；`PortableAccountBindingStore` 已端口本地 `bindings.json` 缓存、服务端绑定列表同步、绑定/更新/解绑、同步状态、权限变更、使用时间记录、按平台过滤历史、最新 100 条历史裁剪、坏 JSON 恢复和原版 PascalCase 字段；`PortableAccountBindingAuthorizationFlow` 已端口账号绑定 OAuth 编排，覆盖授权成功后调用绑定 API 并更新本地缓存、授权失败短路、绑定失败不污染本地缓存和服务端错误 fallback；DI 接线、账号绑定页面和真实服务端 OAuth/绑定联调仍需迁移 | 迁移 |
| 账号注销 | `AccountDeletionViewModel.cs` / `ApiService.RequestDeletionAsync` / `ApiService.CancelDeletionAsync` | `PortableAccountDeletionApiProtocol` / `PortableAuthApiClient` / `PortableAccountDeletionController` 已端口原版 `/api/account/deletion` POST/DELETE 路径、注销请求 JSON 字段、Bearer + HMAC 签名边界、确认文字「确认注销」、默认原因“用户主动注销”、取消分支、失败消息 fallback 和成功后清理本地 token；账号注销页面、关闭应用流程和真实服务端联调仍需迁移 | 迁移 |
| 订阅/卡密 | `SubscriptionService.cs` / `ApiService.GetSubscriptionAsync` / `ApiService.ActivateCardKeyAsync` / `ApiService.GetActivationHistoryAsync` / `ApiService.RenewAccountWithCardKeyAsync` | `PortableSubscriptionApiProtocol` 已端口订阅查询、卡密激活、激活历史、账号续费四个 API 路径，请求 JSON 字段、Bearer 接入边界、统一响应 envelope、订阅信息/激活结果/激活历史解析；`PortableAuthApiClient` 已接入订阅查询、卡密激活、激活历史和账号续费真实 HTTP client 核心；`PortableSubscriptionStore` 已端口本地 `subscription.json` 缓存、remaining days、服务端订阅同步、卡密激活后 `card_key` source 更新、清空缓存和坏 JSON 恢复；`PortableCardKeyActivationMessages` 已端口空卡密提示、`CARDKEY_INVALID` / `CARDKEY_USED` / `NETWORK_ERROR` 中文本地化、服务端未知错误回退、续费成功消息和异常失败前缀；`PortableAccountRenewalController` 已端口原账号续费弹窗控制流，覆盖账号/卡密必填、文本修剪、服务端续费成功消息、失败消息 fallback 和异常前缀；DI 接线、续费弹窗 Avalonia 视图和订阅页面仍需迁移 | 迁移 |
| 服务端公告 | `ServerAuthService.cs` / `ServerAuthInitializer.cs` | `PortableServerHeartbeatService` 已端口 heartbeat request 构建、上一轮 challenge + sessionKey 的 HMAC-SHA256 Base64 签名、原版 JSON 字段序列化、heartbeat JSON 响应解析、公告事件、强制更新事件、无效 token/设备被踢/用户异常的返回登录事件、三类强制登出默认文案、订阅状态、订阅到期时间和 allowed features 映射；`PortableServerHeartbeatApiClient` 已端口 `/v1/auth/heartbeat` POST、`X-Device-Id`、Bearer header、未登录本地拒绝、成功后保存下一轮 challenge、下一轮 challenge response 生成，以及强制登出响应清理本地 token；`PortableHeartbeatLoopPolicy` 已端口心跳前登录态预检、未登录返回登录、成功后失败计数清零、订阅失效返回登录、24 小时内到期 warning 只提示一次、连续失败计数、低失败次数网络不稳定 warning 和第 10 次失败返回登录；`PortableReturnToLoginCoordinator` 已端口返回登录请求防重入和 reset；`PortableServerHeartbeatPresentationPolicy` 已端口系统公告去重、强制更新和强制登出的 UI 无关 action；`PortableServerHeartbeatRunner` 已端口单次 tick 编排、登录态预检、心跳执行、网络异常降级为失败心跳、可配置发送重试、重试成功后不计失败、异常耗尽后降级失败心跳、loop action 分发、presentation action 分发和返回登录防重入；`PortableServerHeartbeatPeriodicService` 已端口真实周期宿主、启动即心跳、周期 tick、重复启动幂等和停止取消；`PortableReturnToLoginNavigationController` 已端口原版返回登录导航顺序，覆盖停止会话服务、清理认证状态、登录前 flush、展示登录、取消登录后退出、登录成功后重启服务和回传用户名 | 迁移 |
| 功能授权 | `ProtectionService.CheckFeatureAuthorizationAsync` / `ServerAuthService.CheckFeatureAuthAsync` | `PortableFeatureAuthorizationService` 已端口本地模式默认放行、服务端模式 checker 注入、缺 checker 拒绝、checker 成功/失败结果映射、异常拒绝、空 featureId 拒绝、原版 5 分钟功能授权缓存、`/api/auth/feature/{featureId}` GET 签名请求、服务端 authorized/expiresAt 响应解析、API checker 接线、失败或未授权响应拒绝、未登录状态本地拒绝且不发网络请求、`AUTH_INVALID` 到返回登录决策映射、可注入订阅状态门禁、未激活/过期订阅先于 checker 拒绝、卡密激活后的有效订阅继续进入功能授权 checker，以及功能受限/订阅失效/返回登录的 UI 无关 action policy；Avalonia UI 承接仍需迁移 | 迁移 |
| SSL Pinning | `SslPinningHandler.cs` | `PortableSslPinningValidator` 已移除 `TMProtect.dll` 依赖并端口跨平台 SPKI SHA-256 Base64 pin 计算、系统 TLS policy error 优先拒绝、叶子证书 pin 匹配、链证书 pin 匹配、缺证书/缺 pin/不匹配失败原因、占位 pin 过滤和 `HttpClientHandler` 回调工厂；`PortableSslPinningServerConfiguration` / `PortableSslPinningRotatingPin` 已端口真实服务端 host 规范化、当前 pin、备份 pin、占位 pin 过滤、去重和按 NotBefore/NotAfter 时间窗启用的证书轮换策略；真实服务端证书 pin 值填充和联调验收仍需迁移 | 迁移 |
| 保护系统 | `ProtectionService.cs` | macOS 不复用 Windows 反调试/Native DLL | 替换 |
| 自动更新 | `AppInfoSettings.cs` / `AppInfoViewModel.cs` | `PortableAppInfoSettings` / `FileAppInfoSettingsStore` / `PortableAppUpdatePolicy` 已端口更新服务器地址、自动检查开关、检查间隔、上次检查时间、当前版本、自动检查到期判断、检查时间回写、版本比较和强制更新决策、`settings.json` 原子保存/读取/坏 JSON 恢复；`PortableAppUpdateProtocol` / `PortableAppUpdateManifest` 已端口服务端更新 manifest JSON 解析；`PortableMacOSAppInstallerPlanner` 已端口 macOS `.dmg` 下载目标与 `/usr/bin/open` 安装打开计划；`PortableAppUpdateDownloader` 已端口 HTTP 下载执行、临时文件写入、SHA-256 校验、成功后覆盖目标文件和 hash 失败清理；`PortableAppInfoCatalog` / `PortableAppInfoReportBuilder` / `PortableAppInfoReportExportPlanner` / `PortableAppInfoReportExporter` 已端口原版功能模块/许可证/版本历史静态数据、应用信息报告文本骨架、默认导出路径命名和报告原子写盘；真实更新服务器联调、更新 UI 和安装器启动后的人工验收仍需迁移 | 迁移 |

## 验收清单

macOS 版本完成前，每一行功能必须至少满足一项：

- 有 macOS 可运行实现。
- 有自动化测试覆盖核心逻辑。
- 有人工验收记录覆盖 UI 行为。
- 有明确降级说明，并被产品范围接受。
- 有服务端依赖说明和配置要求。
