# 天命 macOS M5 人工验收记录

日期：2026-05-14

> 自动化覆盖：1423 个测试全过（本轮 replay 在 `m5-macos-platform` 分支复跑）；本文件记录
> 只能人工验的桌面交互项，以及当前终端 session 无法替代的桌面行为。
>
> Round 3 replay note:
> - `scutil --proxy` live snapshot 已记录到 `/tmp/scutil-sample-on.txt`
> - 代理关闭态 live snapshot 未采集：本轮不主动改动宿主机系统代理
> - `dotnet run --project src/Tianming.Desktop.Avalonia --no-build` 已启动到
>   `AppLifecycle` 日志，但未在本终端里完成顶部系统菜单交互点击

## Keychain（M1 已完整，M5 沿用）

- [ ] 通过 M4 AI Keys 页填入一个 API Key
- [ ] 关闭应用重启，Key 自动从 Keychain 读回
- [ ] 打开 macOS "钥匙串访问" → 登录 → 查找 `tianming-novel-ai-writer` → 看到条目

说明：M5 不动 Keychain 逻辑；`MacOSKeychainApiKeySecretStore` 在 M1 已是完整 shell 实现（`/usr/bin/security`）。

## 系统代理（M5 新增）

- [ ] 关闭 macOS 系统代理开关 → 启动天命 → AI 请求直连（mitmproxy 不拦截）
- [ ] 打开 macOS 系统代理（127.0.0.1:1082 或你的 Clash / Surge 端口）→ 重启天命 →
      AI 请求经代理（在 Clash GUI 看到天命进程的出站流量）
- [ ] 配置 `*.local` 在 Exceptions 里 → 访问 `http://server.local` 不走代理
- [ ] 系统偏好里切换代理开关 → 不重启天命 → 下一次 AI 请求按新设置走（代理是
      `IWebProxy.GetProxy` 每次重新查 `IPortableSystemProxyService`）

实测 smoke：在写代码时跑了 `MacOSSystemProxyService.GetCurrent()` 直接对接当前机器
`/usr/sbin/scutil --proxy`，输出 `HasProxy=True / HTTP=http://127.0.0.1:1082/`，
Exceptions 11 项全部正确解析（包含 `localhost / *.local / 10.0.0.0/8 / tailscale.com`）。

## 系统外观监听（M1 已完整 polling 实现，M5 UI 仍 Light 占位）

- [ ] 系统偏好切换 Light → Dark，3 秒内 monitor / logs 观察到快照变化
- [ ] Dark → Light，3 秒内 monitor / logs 观察到快照变化
- [ ] 后续解除 `ThemeBridge` 的 forced Light 后，再补做真实 UI 跟随验收

说明：`MacOSSystemAppearanceMonitor` 与 `PortableThemeStateController` 已接线，但
`ThemeBridge.ApplyCore()` 和 `App.axaml` 目前仍强制 `ThemeVariant.Light` /
`RequestedThemeVariant="Light"`，所以这轮不能把“UI 已跟随系统”记成通过。

## 应用主菜单（M5 新增）

NativeMenu 挂到 `MainWindow.axaml` 上，Avalonia 11 在 macOS 下自动升级到顶部系统菜单栏。

- [ ] 菜单栏顶部看到"天命 / 文件 / 编辑"三个顶级项
- [ ] **⌘Q** 退出应用（`MainWindowViewModel.QuitCommand` → `desktop.Shutdown()`）
- [ ] **⌘逗号** 跳设置页（`OpenPreferencesCommand` → `NavigateAsync(PageKeys.Settings)`）
- [ ] **⌘N** 跳 Welcome（`NewProjectCommand`；M4 接入对话框前先导航过去）
- [ ] **⌘O** 跳 Welcome（`OpenProjectCommand`；M4 接入文件夹选择对话框后改）
- [ ] **⌘S** 当前是 stub（`SaveCommand` 静默 no-op）；M4 编辑器实装后接保存当前章节
- [ ] 顶级"关于天命"菜单项点一下，应能触发 Console 日志（`AboutCommand`）

## 通过标准

四组能力全部勾选 ✅ 即 M5 验收通过。任何 ❌ 都属于回归，需要回到 plan 修。
