# M0 服务层抽离候选清单

日期：2026-05-10

## 目的

为 M1 跨平台类库抽离选择第一批低风险文件。优先选择纯模型、纯 JSON 文件服务、生成门禁、校验、追踪和打包服务。暂不迁移 WPF View、WPF ViewModel、Windows 系统集成、登录保护系统和 WebView2 爬虫。

## 首批优先抽离

| 组 | 文件或目录 | 理由 | M1 验证方式 |
|---|---|---|---|
| 模型 | `Services/Modules/ProjectData/Models` | 多数是纯数据结构 | 新 classlib 编译通过 |
| 接口 | `Services/Modules/ProjectData/Interfaces` | 可先稳定服务边界 | 新 classlib 编译通过 |
| 生成门禁 | `Services/Modules/ProjectData/Implementations/Generation/GenerationGate.cs` | 核心闭环关键逻辑 | 用 fake CHANGES 输入做通过/失败测试 |
| AI 输出解析 | `Services/Modules/ProjectData/Implementations/Generation/AIOutputParser.cs` | 可独立测试协议解析 | 测试 `---CHANGES---` 与 JSON 缺失场景 |
| 事实快照 | `Services/Modules/ProjectData/Implementations/Tracking` | 维持章节状态连续性的核心 | 用最小章节变更数据测试状态更新 |
| 统一校验 | `Services/Modules/ProjectData/Implementations/Validation` | 核心质量门禁 | 用最小项目数据测试错误报告 |
| 索引服务 | `Services/Modules/ProjectData/Implementations/Indexing` | 多为文件/文本索引逻辑 | 用临时项目目录测试索引输出 |
| 打包发布 | `Services/Modules/ProjectData/Implementations/Packaging` | 生成任务包和发布包关键 | 用临时项目目录测试 manifest/历史输出 |
| 版本追踪 | `Services/Modules/VersionTracking` | 文件注册表逻辑相对独立；`DependencyConfig` 已复用，`FileVersionTrackingService` 已替换原 `StoragePathHelper` / Toast / `TM.App` 绑定，并避免跨平台扫描对 Windows Registry 的误报 | 用临时项目目录测试 registry 读写、依赖快照、过期检测和下游通知 flush |

## 暂缓抽离

| 文件或目录 | 暂缓原因 |
|---|---|
| `Core/App` | WPF 应用生命周期和窗口强绑定 |
| `Framework/UI` | WPF 控件、窗口、工作区强绑定 |
| `Framework/Common/Controls` | WPF 控件和 XAML 强绑定 |
| `Framework/Common/ViewModels` | 大量调度和 UI 状态依赖，需要逐类判断 |
| `Framework/Notifications/SystemNotifications` | 注册表、UWP 通知和 Windows 系统集成 |
| `Services/Framework/SystemIntegration` | 托盘、Windows 通知、Win32 API |
| `Framework/Common/Services/ProtectionService.cs` | Windows 反调试、WMI、TMProtect.dll |
| `Framework/Common/Services/SslPinningHandler.cs` | TMProtect.dll 绑定，需要重写 |
| `Modules/Design/SmartParsing/BookAnalysis/Crawler` | WebView2.Wpf 绑定，需要替换 |

## 候选扫描结果摘要

### ProjectData 服务与接口

优先关注：

- `Services/Modules/ProjectData/Interfaces`
- `Services/Modules/ProjectData/Models`
- `Services/Modules/ProjectData/Implementations/Generation`
- `Services/Modules/ProjectData/Implementations/Validation`
- `Services/Modules/ProjectData/Implementations/Tracking`
- `Services/Modules/ProjectData/Implementations/Indexing`
- `Services/Modules/ProjectData/Implementations/Packaging`

扫描中出现的服务包括：

- `ChangeDetectionService`
- `ContextService`
- `FocusContextService`
- `GeneratedContentService`
- `GenerationStatisticsService`
- `GuideContextService`
- `DataIndexService`
- `IndexService`
- `KeywordChapterIndexService`
- `PlotPointsIndexService`
- `ModuleEnabledService`
- `PackageHistoryService`
- `PublishService`
- `WorkScopeService`
- `GlobalSummaryService`
- `ProgressiveSummaryService`
- `CharacterStateService`
- `ConflictProgressService`
- `FactionStateService`
- `ForeshadowingStatusService`
- `ItemStateService`
- `LedgerTrimService`
- `LocationStateService`
- `RelationStrengthService`
- `TimelineService`
- `UnifiedValidationService`
- `ValidationReportService`
- `ValidationSummaryService`

### 模块服务

优先关注可脱离 ViewModel 的 Service 文件：

- `CharacterRulesService`
- `FactionRulesService`
- `LocationRulesService`
- `PlotRulesService`
- `WorldRulesService`
- `BookAnalysisService`
- `EssenceChapterSelectionService`
- `CreativeMaterialsService`
- `ContentConfigService`
- `BlueprintService`
- `ChapterService`
- `VolumeDesignService`
- `OutlineService`
- `ChapterRepairService`

暂缓：

- 所有 `*ViewModel.cs`
- `WebCrawlerService.cs`
- `NovelCrawlerService.cs`

### AI Core

优先关注：

- `Services/Framework/AI/Interfaces`
- `Services/Framework/AI/Core/ApiKeyRotationService.cs`：已端口到 `src/Tianming.AI`，用 `Action<string>` 日志注入替换 `TM.App.Log`，覆盖轮换、排除、冷却、永久禁用、状态查询；`ConfiguredAITextGenerationService` 非流式/流式生成已可选接入轮换 key、成功上报和 429 后排除当前 key 重试。
- `Services/Framework/AI/Core/AIService.cs` 中的模型库与用户配置子集：已端口到 `src/Tianming.AI` 的 `FileAIConfigurationStore`，用显式 library/configurations 目录替换 `StoragePathHelper`，覆盖模型库加载、ProviderModels fallback、配置 CRUD、单 active 配置、ApiKey 不落盘；`IApiKeySecretStore` / `MacOSKeychainApiKeySecretStore` 已提供 macOS Keychain 真值存取边界。
- `Services/Framework/AI/Monitoring/StatisticsService.cs`：已端口到 `src/Tianming.AI` 的 `FileUsageStatisticsService`，用显式文件路径替换 `StoragePathHelper`，覆盖记录、汇总、每日、模型聚合、最近记录、裁剪、清空。
- `Modules/AIAssistant/PromptTools/PromptManagement/Services/PromptService.cs` 的文件存储子集：已端口到 `src/Tianming.AI` 的 `FilePromptTemplateStore`，用显式 categories/templates/built_in_templates 路径替换 `ModuleServiceBase` / `StoragePathHelper` / `TM.App.Log`，覆盖内置模板合并、用户模板覆盖、CRUD、变更事件和内置模板保护。
- `Modules/AIAssistant/PromptTools/VersionTesting/Services/VersionTestingService.cs` 的文件存储子集：已端口到 `src/Tianming.AI` 的 `FilePromptVersionStore`，用显式 test_versions 路径替换 `StoragePathHelper` / `TM.App.Log`，覆盖加载排序、CRUD、测试结果字段更新、清空和临时文件保存；版本测试执行与双提示词对比已端口到 `PromptVersionTestRunner`，用 `IPromptTextGenerator` 替换 UI ViewModel 直接调 AI。
- `Framework/Common/Helpers/AI/PromptGenerationService.cs` 的可移植核心：已端口到 `src/Tianming.AI` 的 `PromptGenerationCoreService`，用 `IPromptTextGenerator` 替换 `AIService` 直接依赖，覆盖输入校验、元提示词构建、AI 失败映射、JSON/纯文本返回解析、tags 解析；`OpenAICompatiblePromptTextGenerator` 与 `ConfiguredAITextGenerationService` 已提供 HTTP 接线适配器。
- `Services/Framework/AI/Core/AIService.cs` / `Services/Framework/AI/SemanticKernel/SKChatService.cs` 的 OpenAI-compatible HTTP 子集：已端口到 `src/Tianming.AI` 的 `OpenAICompatibleChatClient`，覆盖 `/v1/chat/completions` URL 规范化、Bearer / `X-Api-Key` / Cherry Studio 兼容头、非流式 message content/usage 解析、SSE `data:` delta content 流式解析、HTTP error payload 映射、模型名前缀与 `-free` 清理；`ConfiguredAITextGenerationService` 已把 active configuration、Provider endpoint、Model name、DeveloperMessage、ApiKey、可选多 Key 轮换和非流式/流式调用统计回写串到非流式/流式文本生成；SemanticKernel 和 Anthropic/Gemini/Azure 多协议接线仍需后续迁移。
- `Services/Framework/AI/SemanticKernel/Conversation/Config/ModeProfileRegistry.cs` 的配置子集：已端口到 `src/Tianming.AI` 的 `ConversationModeProfileCatalog`，先不绑定 mapper，覆盖 Ask/Plan/Agent 模式目录、payload target、raw content 显示策略、执行引擎需求和未知模式回落。
- `Services/Framework/AI/SemanticKernel/Conversation/Mapping/AskModeMapper.cs`、`PlanModeMapper.cs` 与 `AgentModeMapper.cs`：已端口到 `src/Tianming.AI`，用轻量 `ConversationMessage` / `ConversationRole` 替换 SK `AuthorRole`，覆盖 raw answer、thinking 分析块、Plan payload/summary/parse-failed、Agent payload 映射和基于 `content_guide*.json` 的快速计划。
- `Services/Framework/AI/SemanticKernel/Conversation/Thinking/Strategies/TagBasedStrategy.cs` 与 `ThinkingBlockParser.cs`：已端口到 `src/Tianming.AI` 的 `TagBasedThinkingStrategy` / `ThinkingBlockParser`，用字符串输入替换 SK chunk 直接依赖，覆盖 thinking/answer 标签分流、未闭合标签分片保留、噪声行过滤和分析块拆分。
- `Services/Framework/AI/SemanticKernel/Conversation/Helpers/ExecutionTraceCollector.cs`：已端口到 `src/Tianming.AI`，用显式 `Record(ExecutionEvent)` 替换全局 `ExecutionEventHub` 订阅和 `TM.App.Log`，覆盖工具调用轨迹聚合、排序、耗时、失败统计和用户友好失败原因。
- `Services/Framework/AI/SemanticKernel/ChatHistoryCompressionService.cs`：已端口到 `src/Tianming.AI`，用 `PortableChatMessage` 替换 SemanticKernel `ChatHistory`，覆盖 90% 上下文触发、rolling memory 生成、结构化记忆合并、最近轮次保留、摘要失败硬截断。
- `Services/Framework/AI/SemanticKernel/Embedding/ITextEmbedder.cs`：已端口为 `src/Tianming.AI` 的可注入 `ITextEmbedder` 边界，并新增纯托管 `HashingTextEmbedder` 作为无 ONNX 依赖的本地相似度实现。
- `Services/Framework/AI/SemanticKernel/LocalEmbeddingService.cs`

需要平台化后再迁移：

- `Services/Framework/AI/Core/AIService.cs` 的完整配置装配、Key 编辑 UI 接线、真实 Keychain 人工验收和业务会话状态。
- `Services/Framework/AI/SemanticKernel/SKChatService.cs` 的完整 SemanticKernel 构建、Agent plugin、压缩服务接线、UI 调度和多协议 provider 装配。
- `Services/Framework/AI/SemanticKernel/VectorSearchService.cs`：本地相似度搜索和无模型/无依赖时的 keyword fallback 已端口到 `src/Tianming.AI` 的 `FileVectorSearchService`，覆盖章节 Markdown 分块、相关度排序、按章节取片段、删除后缓存失效；章节生成后索引更新已通过 `src/Tianming.ProjectData` 的 `IChapterDerivedIndex` 和 `src/Tianming.AI` 的 `FileVectorChapterIndexAdapter` 接入；ONNX 真向量、KernelMemory/OpenAI 混合模式、模型路径与 macOS OnnxRuntime 包仍需后续验证。

## M1 第一条实现线

1. 新建 `src/Tianming.ProjectData` 类库。
2. 迁移 `Services/Modules/ProjectData/Models` 和 `Services/Modules/ProjectData/Interfaces`。
3. 引入 `IProjectStorage` 抽象，替换直接依赖 `StoragePathHelper` 的服务。
4. 迁移 `AIOutputParser` 和 `GenerationGate`。
5. 新建测试项目，覆盖协议解析、引用校验和最小生成门禁失败路径。
